//main.cpp
#include "Class.H"
#include <iostream>
#include <vector>
using namepace std;

int main() {
	Class mow();
	Class mow2= {3, 'c'};
	Class mow3= {10, 'a'};
	vector <Class> z;
	v.push_back (mow);
	v.push_back (mow3);
	v.push_back (mow2);
	for (auto x :: z) {
		cout << x.GetYear() << x.GetStream() << endl;
	}
return 0;
}
